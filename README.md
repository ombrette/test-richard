#### Start by running `git clone https://github.com/ombrette/test-richard.git`

## How to Run
1. Make sure you have installed [gulp-cli](https://gulpjs.com/)
1. on *root* folder run `npm install`
1. Run `gulp`
